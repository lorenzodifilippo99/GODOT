{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Dynamics/Batched",
    "description": "Configuration for Batched (DynamicDispatcher) dynamics in dynamics plugin",
    "type": "array",
    "minItems": 1,
    "items": {
        "description": "Configuration of a batch",
        "type": "object",
        "additionalProperties": false,
        "required": [
            "epoch",
            "model"
        ],
        "properties": {
            "epoch": {
                "$ref": "../../../definitions.json#/definitions/epoch",
                "description": "Batch start time. Batches must be chronologically ordered."
            },
            "model": {
                "description": "Name of a dynamic model to be used in this batch.",
                "type": "string"
            }
        }
    }
}