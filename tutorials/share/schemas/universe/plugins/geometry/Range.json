{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Geometry/Range",
    "description": "Range [km] from a target point to an observer point.",
    "type": "object",
    "required": [
        "observer",
        "target"
    ],
    "additionalProperties": false,
    "properties": {
        "target": {
            "description": "Target of the Range, a point in the frames system",
            "type": "string"
        },
        "observer": {
            "description": "Observer of the Range, a point in the frames system",
            "type": "string"
        }
    }
}