{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Geometry/RangeRate",
    "description": "Range rate [km/sec] from a target point to an observer point.",
    "type": "object",
    "required": [
        "observer",
        "target"
    ],
    "additionalProperties": false,
    "properties": {
        "target": {
            "description": "Target of the Range rate, a point in the frames system",
            "type": "string"
        },
        "observer": {
            "description": "Observer of the Range rate, a point in the frames system",
            "type": "string"
        }
    }
}