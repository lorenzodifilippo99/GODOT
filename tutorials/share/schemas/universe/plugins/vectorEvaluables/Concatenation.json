{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/VectorEvaluables/Concatenation",
    "description": "Concatenation of one or more existing scalar or vector time evaluables",
    "type": "object",
    "required": [
        "evaluables"
    ],
    "additionalProperties": false,
    "properties": {
        "evaluables": {
            "type": "array",
            "description": "Ordered list of scalar or vector time evaluables to concatenate",
            "items": {
                "description": "name of a scalar or vector time evaluable. Must be either in evaluables book or in vectorEvaluables book (cannot be in both)",
                "type": "string"
            }
        }
    }
}