{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Atmosphere/averagedEarth",
    "description": "Configuration for average Earth model in atmosphere plugin",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "point",
        "axes",
        "activity"
    ],
    "properties": {
        "point": {
            "description": "Center of the body to which the atmosphere is attached",
            "type": "string"
        },
        "axes": {
            "description": "Axes to which the atmosphere is attached",
            "type": "string"
        },
        "activity": {
            "description": "Activity parameter",
            "type": "string",
            "enum": [
                "low",
                "medium",
                "high"
            ]
        },
        "radius": {
            "description": "Optional radius to use for computing altitude",
            "type": "string"
        },
        "flattening": {
            "description": "Optional flattening to for computing altitude",
            "type": "number"
        }
    }
}