{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Atmosphere/patchedExponential",
    "description": "Configuration for patched exponential model in atmosphere plugin",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "point",
        "axes",
        "file"
    ],
    "properties": {
        "point": {
            "description": "Center of the body to which the atmosphere is attached",
            "type": "string"
        },
        "axes": {
            "description": "Axes to which the atmosphere is attached",
            "type": "string"
        },
        "file": {
            "description": "Json file with height, density, scale data",
            "type": "string"
        },
        "radius": {
            "description": "Optional radius to use for computing altitude",
            "type": "string"
        },
        "flattening": {
            "description": "Optional flattening to for computing altitude",
            "type": "number"
        }
    }
}