{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Frames/DirFixed",
    "description": "Configuration for DirFixed directions in frames plugin",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "axes",
        "vector"
    ],
    "properties": {
        "axes": {
            "description": "Name of the axes in frames in which the direction vector is given",
            "type": "string"
        },
        "vector": {
            "description": "Fixed direction vector in axes frame (three vector components)",
            "$ref": "../../../definitions.json#/definitions/vector3"
        }
    }
}