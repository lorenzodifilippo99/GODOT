{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Universe/Frames/DirProjected",
    "description": "Configuration for DirProjected directions in frames plugin (i.e. direction projected onto plane defined by its normal direction)",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "dir",
        "planeNormalDir"
    ],
    "properties": {
        "dir": {
            "description": "Name of direction to project",
            "type": "string"
        },
        "planeNormalDir": {
            "description": "Name of direction defining the normal to the projection plane",
            "type": "string"
        }
    }
}